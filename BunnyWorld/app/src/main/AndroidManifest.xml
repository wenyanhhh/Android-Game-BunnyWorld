<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.stanford.cs108.finalproj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StartPopActivity"
            android:theme="@style/AppTheme.Pop" />
        <activity android:name=".GameActivity" />
        <activity android:name=".EditorActivity" />
        <activity
            android:name=".PageCreateActivity"
            android:theme="@style/AppTheme.Pop" />
        <activity
            android:name=".PageChangeActivity"
            android:label="@string/title_activity_page_change"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>